name: Apply 0032 backend settings ownership

on:
  push:
    branches:
      - audit/0032-auxiliary-settings-consistency
    paths:
      - .github/workflows/apply-0032-backend-settings-ownership.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: [self-hosted, v2rayn-widget-ci]
    steps:
      - name: Checkout exact audit branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: audit/0032-auxiliary-settings-consistency
          fetch-depth: 1

      - name: Apply exact backend ownership patch
        shell: powershell
        run: node scripts/apply-0032-backend-settings-ownership.mjs

      - name: Remove one-shot backend ownership machinery
        shell: powershell
        run: |
          Remove-Item scripts/apply-0032-backend-settings-ownership.mjs
          Remove-Item .github/workflows/apply-0032-backend-settings-ownership.yml

      - name: Commit backend ownership fix
        shell: powershell
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "0032: preserve authoritative live settings on full save"
          git push origin HEAD:audit/0032-auxiliary-settings-consistency
