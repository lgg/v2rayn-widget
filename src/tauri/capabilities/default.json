{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the proxy client widget",
  "windows": ["main", "settings", "debug", "happ-setup"],
  "permissions": [
    "core:default"
  ]
}
